Script	Description

basic_recon.sh -- Performs DNS lookup, traceroute, ping sweep, and port scan using Nmap.

check_open_ports.py	-- Scans a host for open ports and prints banner info.

fail2ban_status.sh -- Shows current bans, top offending IPs, and jail statuses.

password_strength_checker.py -- Checks password entropy and estimated crack time.

suspicious_processes.sh -- Lists processes using odd ports or high privileges unexpectedly.
